<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Estilo para el diálogo de carga -->
    <style name="ThemeOverlay.TuBarrio.LoadingDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Estilo para la pantalla de splash -->
    <style name="Theme.TuBarrio.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
