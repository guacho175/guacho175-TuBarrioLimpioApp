# Required: public HTTPS base URL of the REST API. It must end with "/".
API_BASE_URL=https://api.example.com/api/

# Required for map rendering. Create an Android-restricted key for the
# application package and signing-certificate fingerprint.
MAPS_API_KEY=replace_with_restricted_google_maps_key
